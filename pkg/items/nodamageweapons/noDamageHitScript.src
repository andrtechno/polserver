use uo;
use os;

include ":attributes:attributes";

program noDamageHitScript(attacker,defender,item)

    //SendSysMessage( attacker, "noDamageHitScript");
    SkillCheck(attacker, item.Attribute, -1, "", ADV_ALL);
    return 1;

endprogram
