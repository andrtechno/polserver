use uo;

include "include/objtype";

program textcmd_spellbook( who )
    var book := CreateItemInBackpack(who,UOBJ_SPELLBOOK, 1);
    var i;
    for (i := 0x1f2d; i <= 0x1f72; i := i + 1)
        CreateItemInContainer(book,  i, 1);
    endfor
endprogram