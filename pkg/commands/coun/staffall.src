use uo;
use unicode;

program textcmd_staffall(who, text)

    foreach character in EnumerateOnlineCharacters()
        if( who.cmdlevel > 0)
            SendSysMessageUC( character, who.name + ": " +text, "RUS", 3, 54 );
        endif
    endforeach

    return 1;
endprogram
