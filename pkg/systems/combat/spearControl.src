use uo;
use os;
use cfgfile;

program spearControl(item)
  var cfgfile  := ReadConfigFile(":combat:itemdesc");
  var dmg      := cfgfile[item.objtype].damage;
  if(dmg)
    item.hitscript := "spearHitScript";
  endif




endprogram